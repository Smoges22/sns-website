import Link from "next/link";
import { interactiveCardClass, Card, PageHero, PageShell, SectionContainer } from "@/components/section";
import { createPageMetadata, site } from "@/lib/site";

export const metadata=createPageMetadata({title:"Contact SNS",description:"Contact Sosena Nursing Solutions about RN assessments and individualized care plans. Do not send medical records through public email.",path:"/contact"});
export default function ContactPage(){return <PageShell><PageHero eyebrow="Contact SNS" title="Let's clarify the next clinical step" intro={`Contact ${site.provider} about an RN assessment, individualized care plan, or upcoming Adult Family Home care transition.`}><Link className="inline-flex min-h-12 items-center justify-center rounded-xl bg-navy px-5 py-3 text-sm font-extrabold text-white" href="/request-assessment">Request an Assessment</Link></PageHero><SectionContainer className="bg-soft"><div className="grid gap-6 md:grid-cols-2"><a className={`${interactiveCardClass} block p-6 sm:p-8`} href={`tel:${site.phone.replaceAll("-","")}`}><p className="text-xs font-black uppercase tracking-[.16em] text-teal">Phone</p><h2 className="mt-3 text-3xl font-black text-navy">{site.phone}</h2><p className="mt-4 leading-7 text-slate">Call to discuss the type of service needed and scheduling availability. Leave only basic contact information in voicemail.</p></a><a className={`${interactiveCardClass} block p-6 sm:p-8`} href={`mailto:${site.primaryEmail}`}><p className="text-xs font-black uppercase tracking-[.16em] text-teal">Email</p><h2 className="mt-3 break-all text-2xl font-black text-navy">{site.primaryEmail}</h2><p className="mt-4 leading-7 text-slate">Email basic contact and scheduling information. Do not email detailed medical records until SNS provides separate sharing instructions.</p></a></div><Card className="mt-8 !bg-[#EDF6FA]"><h2 className="text-2xl font-black text-navy">Washington service area</h2><p className="mt-3 leading-7 text-slate">SNS serves clients in Washington State. Availability is confirmed based on the service location, access, clinical need, and scheduling.</p></Card></SectionContainer></PageShell>}

import Link from "next/link";
import { interactiveCardClass, FinalCta, PageHero, PageShell, SectionContainer } from "@/components/section";
import { createPageMetadata, processSteps, site } from "@/lib/site";

export const metadata=createPageMetadata({title:"How RN Assessments & Care Plans Work",description:"See how Sosena Nursing Solutions coordinates a request, RN assessment, individualized care plan, and delivery to the authorized party.",path:"/how-it-works"});
export default function HowItWorksPage(){return <PageShell><PageHero eyebrow="How it works" title="A straightforward path from request to completed documentation" intro={`SNS coordinates with authorized contacts, keeps communication clear, and explains what is needed at each step. Clinical services are provided by ${site.provider}.`}><Link className="inline-flex min-h-12 items-center justify-center rounded-xl bg-navy px-5 py-3 text-sm font-extrabold text-white" href="/request-assessment">Start a Request</Link></PageHero><SectionContainer eyebrow="Four-step process" title="Clear next steps from the beginning"><ol className="relative grid gap-9 md:grid-cols-2 lg:grid-cols-4 lg:gap-10"><span aria-hidden="true" className="absolute left-[12.5%] right-[12.5%] top-6 hidden h-px bg-navy/20 lg:block" />{processSteps.map((step,index)=><li className="relative" key={step.title}><span className="relative z-10 grid h-12 w-12 place-items-center rounded-full bg-navy font-black text-white ring-8 ring-[#FCFDFD]">{index+1}</span><h2 className="mt-6 text-2xl font-black text-navy">{step.title}</h2><p className="mt-3 leading-7 text-slate">{step.text}</p></li>)}</ol></SectionContainer><SectionContainer className="bg-soft" eyebrow="Privacy by design" title="Public intake stays intentionally non-clinical"><div className="grid gap-6 md:grid-cols-3">{[["Basic coordination only","The request form asks for contact, organization, service, timing, and a brief non-clinical message."],["Separate record sharing","Do not send clinical records through the public website or ordinary email. SNS provides separate instructions when records are needed."],["Authorized delivery","Completed documents are provided only to the authorized party using the agreed delivery method."]].map(([title,text])=><div className={`${interactiveCardClass} p-6 sm:p-7`} key={title}><h2 className="text-xl font-black text-navy">{title}</h2><p className="mt-3 leading-7 text-slate">{text}</p></div>)}</div></SectionContainer><FinalCta /></PageShell>}

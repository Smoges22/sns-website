import Link from "next/link";
import { interactiveCardClass, FinalCta, PageHero, PageShell, SectionContainer } from "@/components/section";
import { resourcePath, resources } from "@/lib/resources";
import { createPageMetadata } from "@/lib/site";
export const metadata = createPageMetadata({ title: "RN Assessment & AFH Care Resources", description: "Concise guides about RN assessments, negotiated care plans, annual reviews, and changes in condition for Adult Family Home care.", path: "/resources" });
export default function ResourcesPage() { return <PageShell><PageHero eyebrow="Resources" title="Practical guidance for Adult Family Home care decisions" intro="A small collection of clear, useful guides about RN assessments, care plans, and clinical review."/><SectionContainer><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{resources.map((resource)=><article className={`${interactiveCardClass} flex h-full flex-col p-6 sm:p-7`} key={resource.slug}><p className="text-xs font-black uppercase tracking-[.16em] text-teal">SNS Guide</p><h2 className="mt-4 text-2xl font-black leading-tight tracking-[-.02em] text-navy">{resource.title}</h2><p className="mt-4 flex-1 leading-7 text-slate">{resource.description}</p><Link className="mt-6 inline-flex min-h-11 w-fit items-center font-extrabold text-teal underline decoration-teal/30 underline-offset-4 hover:text-navy" href={resourcePath(resource)}>Read the guide<span aria-hidden="true" className="ml-2">→</span></Link></article>)}</div></SectionContainer><FinalCta /></PageShell>; }

import Link from "next/link";
import { interactiveCardClass, Card, FinalCta, PageHero, PageShell, SectionContainer } from "@/components/section";
import { audiences, createPageMetadata, site } from "@/lib/site";

export const metadata = createPageMetadata({ title: "Who SNS Serves", description: "RN assessment and care-plan support for Adult Family Homes, referral professionals, hospitals, assisted living, and families in Washington.", path: "/who-we-serve" });
export default function WhoWeServePage(){return <PageShell><PageHero eyebrow="Who we serve" title="Clinical clarity across care transitions" intro={`SNS works with authorized providers, professionals, care teams, and families who need a reliable understanding of an adult's current care needs. Services are provided by ${site.provider}.`}><Link className="inline-flex min-h-12 items-center justify-center rounded-xl bg-navy px-5 py-3 text-sm font-extrabold text-white" href="/request-assessment">Request an Assessment</Link></PageHero><SectionContainer><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{audiences.map((audience,index)=><article className={`${interactiveCardClass} p-6 sm:p-7`} key={audience.title}><span className="grid h-10 w-10 place-items-center rounded-full border border-[#D2E1E7] bg-[#F1F7F9] text-xs font-black text-navy">0{index+1}</span><h2 className="mt-4 text-2xl font-black text-navy">{audience.title}</h2><p className="mt-4 leading-7 text-slate">{audience.text}</p></article>)}</div><Card className="mt-12 border-l-4 !border-l-teal !bg-[#F4F9FA] sm:p-8"><h2 className="text-2xl font-black text-navy">Clinical assessment — not placement</h2><p className="mt-3 max-w-4xl leading-7 text-slate">SNS documents assessed care needs so the receiving Adult Family Home can determine whether it can appropriately meet those needs. SNS does not act as a placement agency or make the admission decision.</p></Card></SectionContainer><FinalCta /></PageShell>}
